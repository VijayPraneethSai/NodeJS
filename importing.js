// const var1 = require("./exporting");
// console.log(var1);

// we are destructuring propName
const {propName} = require("./exporting");
// console.log(propName); can do this also direclty but if we want in a better way then,
 
console.log(JSON.stringify(propName)); 

